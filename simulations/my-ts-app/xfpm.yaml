version: "1.0"
globals:
    react_version: "^19.2.4"
    ts_version: "^5.9.2"

catalogs:
    default:
        react: $react_version
        react-dom: $react_version
        typescript: $ts_version
    frd:
        react: $react_version
        react-dom: $react_version
        typescript: $ts_version
        vite: $vite_ver

